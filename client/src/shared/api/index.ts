import { ORIGIN, ENDPOINTS } from "./constants";
export { ORIGIN, ENDPOINTS };

import { getCookie, setCookie } from "./helpers";
export { getCookie, setCookie };

import api from "./interceptor-api";
export default api;
